<Page x:Class="ProjetaHDR.UI.Views.FamiliesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ProjetaHDR.UI.Views"
      xmlns:viewModel="clr-namespace:ProjetaHDR.UI.ViewModels"
      d:DataContext="{d:DesignInstance viewModel:FamiliesViewModel, IsDesignTimeCreatable=True}"
      mc:Ignorable="d">

    <Grid Background="DarkGray">
        <!--<StackPanel Margin="10">
            <Button Content="Atualizar" Command="{Binding RefreshCommand}" Width="100" Margin="0 0 0 10"/>
            <ListBox ItemsSource="{Binding Families}" DisplayMemberPath="Name"/>
        </StackPanel>-->

        <StackPanel Margin="10">
            <Button Content="Atualizar" Command="{Binding RefreshCommand}" Width="100" Margin="0 0 0 10"/>

            <!-- Layout de exibição das famílias -->
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Families}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" BorderBrush="Gray" Margin="5" Padding="5" CornerRadius="5">
                                <StackPanel>
                                    <Image Source="{Binding ThumbnailPath}" Width="100" Height="100" Stretch="Uniform"/>
                                    <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </StackPanel>
    </Grid>

</Page>
