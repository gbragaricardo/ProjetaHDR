<Page x:Class="ProjetaHDR.UI.Views.FamiliesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ProjetaHDR.UI.Views"
      xmlns:viewModel="clr-namespace:ProjetaHDR.UI.ViewModels"
      d:DataContext="{d:DesignInstance viewModel:FamiliesViewModel, IsDesignTimeCreatable=True}"
      mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/FamiliesSearchBoxTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    
    <Border Padding="15"
            Background="White">
        
        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel HorizontalAlignment="Center"
                        MinWidth="230"
                        Width="Auto">
                
                <TextBox Text="{Binding Search, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="Auto"
                         Height="40"
                         Margin="5"
                         Style="{StaticResource SearchFamiliesBox}"/>
                
                <Button Grid.Row="0"
                        Content="Atualizar"
                        Command="{Binding RefreshCommand}"
                        Width="100"
                        Margin="0 15"
                        VerticalAlignment="Bottom"/>
            </StackPanel>
            

            <!-- Layout de exibição das famílias -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          HorizontalAlignment="Center">

                
                            
                <ItemsControl ItemsSource="{Binding Families, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="1" 
                                    BorderBrush="Black"
                                    Margin="5" Padding="10"
                                    CornerRadius="5"
                                    Width="230">

                                <StackPanel>
                                    <Button Command="{Binding DataContext.DownloadCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Width="30" Height="30"
                                            Background="Transparent"
                                            HorizontalAlignment="Left"
                                            HorizontalContentAlignment="Center"
                                            VerticalContentAlignment="Center">

                                        <Image Source="{Binding DataContext.ImagePath, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                               Width="16" Height="16"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>

                                    </Button>

                                    <Image Source="{Binding ThumbnailPath}" 
                                           Width="128" 
                                           Height="128" 
                                           Stretch="Uniform"/>

                                    <DockPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="Bold"
                                                   TextWrapping="Wrap"
                                                   />
                                    </DockPanel>
                                    

                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>
            </ScrollViewer>
        
        </Grid>
    </Border>

</Page>
